# Never let Spring integration tests run Liquibase against the developer database.
spring.datasource.url=${TEST_DB_URL:jdbc:mysql://localhost:3306/interview_db_test?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
