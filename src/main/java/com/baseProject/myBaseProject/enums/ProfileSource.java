package com.baseProject.myBaseProject.enums;

/**
 * Whether a candidate profile still reflects the AI parse result or has been
 * edited by its owner.
 */
public enum ProfileSource {
    AUTO_PARSED,
    USER_EDITED
}
